server:
  address: localhost
  port: 8080

spring:
  profiles:
    active: db
  datasource:
    driver-class-name: ${driver-class-name}
    url : ${url}
    username: ${username}
    password: ${password}

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    database: mysql
    show-sql: true

  jwt:
    secret: 4dc8a15a83350e03f1dbc83fbb71134b0c0889c22c756e7b55e8ed60327dfa85
